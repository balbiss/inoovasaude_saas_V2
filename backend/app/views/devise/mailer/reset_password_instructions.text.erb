Olá, <%= @resource.first_name.presence || @resource.email %>!

Recebemos uma solicitação para redefinir a senha da sua conta no <%= ENV.fetch('APP_NAME', 'VisitAIA CRM') %>.

Acesse o link abaixo para criar uma nova senha:
<%= @frontend_reset_url %>

Este link expira em 6 horas.

Se você não solicitou a redefinição de senha, ignore este e-mail.
